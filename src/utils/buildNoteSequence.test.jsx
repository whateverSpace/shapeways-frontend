const {
  makeNotesFromSegmentData
} = require('./buildNoteSequence.js');

describe('tests for makeNotesFromSegmentData function', () => {
  it('should return a single color for complements of 0', () => {
    const
    makeNotesFromSegmentData (segmentsCounter)
  });
